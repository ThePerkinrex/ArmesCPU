pub mod literal; // Literal (as you read) elf types

pub struct ElfHeader {}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
